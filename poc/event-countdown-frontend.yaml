apiVersion: platform.example.io/v1alpha1
kind: AppFrontend
metadata:
  name: event-countdown
spec:
  image:
    repository: us-central1-docker.pkg.dev/spaces-playground/demo-platform/event-countdown
    tag: v1.0.1
  containerPort: 8080
  replicas: 1
  servicePort: 80
  healthCheck:
    path: /health
    port: 8080
    initialDelaySeconds: 10
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi
